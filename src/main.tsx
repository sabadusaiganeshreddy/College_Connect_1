import React from 'react'
import ReactDOM from 'react-dom/client'
import CollegeConnect from './CollegeConnect.tsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <CollegeConnect />
  </React.StrictMode>,
)
